# Railway Nixpacks configuration for Discord Music Bot

[phases.setup]
nixPkgs = ["nodejs", "python3", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "PYTHON=$(which python3) node src/index.js"
