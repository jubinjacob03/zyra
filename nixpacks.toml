# Railway Nixpacks configuration for Discord Music Bot

[phases.setup]
nixPkgs = ["nodejs", "python3", "ffmpeg", "gcc", "gnumake", "libopus", "yt-dlp"]

[phases.install]
cmds = ["npm install --ignore-scripts", "npm rebuild @discordjs/opus"]

[start]
cmd = "node src/index.js"
